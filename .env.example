# =============================================================================
# Estalara – Book Demo Form Backend
# =============================================================================
# Copy this file to .env.local and fill in the values.

# ---------------------------------------------------------------------------
# Supabase (database for demo requests)
# Create a free project at https://supabase.com
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------------------------------------------------------------------------
# Resend (email notifications + contact storage)
# Create a free account at https://resend.com
# ---------------------------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxx

# The email address to receive demo request notifications.
#
# IMPORTANT (Free Tier / No Verified Domain):
#   Without a verified domain in Resend, you can ONLY send emails to the
#   Resend account owner's email address. Set this to that email.
#   To send to any email, verify your domain at https://resend.com/domains
#
NOTIFICATION_EMAIL=you@example.com

# The "from" address for notification emails.
#
# IMPORTANT: This MUST use your verified Resend domain as the sender.
#   Our verified domain in Resend is: contact.estalara.com
#   So the from address must be: Estalara <noreply@contact.estalara.com>
#   (or any other local-part @contact.estalara.com)
#
#   Without a verified domain, only onboarding@resend.dev works (test mode).
#
RESEND_FROM_EMAIL=Estalara <noreply@contact.estalara.com>

# Resend Audience ID for storing demo request contacts.
# Find this at https://resend.com/audiences – click on your audience to see its ID.
RESEND_AUDIENCE_ID=your-audience-id

# ---------------------------------------------------------------------------
# Google Search Console (site ownership verification)
# ---------------------------------------------------------------------------
# To find your verification code:
#   1. Go to https://search.google.com/search-console
#   2. Select your property (www.estalara.com)
#   3. Go to Settings → Ownership verification → HTML tag
#   4. Copy ONLY the "content" value from the meta tag Google gives you
#      (e.g. if Google shows <meta name="google-site-verification" content="abc123" />
#       then set this variable to: abc123)
#
# This is ONLY needed for verification. Google Search Console does NOT require
# any tracking script — it uses Google's own crawl and search data.
#
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code
